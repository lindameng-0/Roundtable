# Roundtable backend - minimal deps for FastAPI + Supabase + LiteLLM (no conda/file paths)
fastapi==0.134.0
uvicorn==0.41.0
python-dotenv==1.2.1
supabase==2.28.0
pydantic>=2.12.0
httpx==0.28.1
python-multipart==0.0.22
litellm>=1.0.0
tiktoken==0.12.0
google-genai>=0.3.0
python-docx==1.2.0
pymupdf==1.25.4
asyncpg==0.30.0

# Official MCP SDK; keep the tested v1 API until an explicit migration.
mcp==1.30.0
