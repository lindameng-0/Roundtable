# Allow large manuscript payloads (fixes HTTP 413 when pasting/uploading long text).
# If your deployment uses nginx and includes this file, set client_max_body_size.
# Emergent base image / other proxies may use a 1MB default; 50M allows full manuscripts.

client_max_body_size 50M;
